apply plugin: 'java'
apply plugin: 'maven'

group = 'br.com.bea'
version = '0.0.2'

description = """B&A Tecnologia and Collaborators - Android Tools"""

sourceCompatibility = 1.5
targetCompatibility = 1.5

repositories {
        
     maven { url "http://nexus.codehaus.org/content/groups/public/" }
     maven { url "http://nexus.codehaus.org/content/groups/snapshots-group/" }
     maven { url "http://repository.sonatype.org/content/groups/forge/" }
     maven { url "http://maven.java.net/content/groups/public/" }
     maven { url "http://repo.maven.apache.org/maven2" }
}

dependencies {
    compile group: 'com.google.android', name: 'support-v4', version:'r7'
    compile group: 'junit', name: 'junit', version:'4.11'
    compile(group: 'com.google.android', name: 'android', version:'4.1.1.4') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
